// forEach method  allows to run a function for every element of the array.
let arr=["siva","vamsi","subba"]
arr.forEach((item,index,arr)=>{
    console.log(item+" avaliable at index "+index+" in array "+arr);
});