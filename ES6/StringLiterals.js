// Template literals (Template Strings)(String literal)
//Template Literal in ES6 provides new features to create a string that gives more control over dynamic strings.
// that allow for easier string interpolation.
// hey are denoted by backticks (`) instead of single or double quotes
let s="siva";

let a=10;
console.log(`${s} is good at java and rated as ${a}`);


const tableValue=4;
for(var i=1;i<=10;i++){
    console.log(`${tableValue} * ${i} = ${tableValue * i}`)
}